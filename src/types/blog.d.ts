type PartialBlog = {
  title: string;
  description: string;
  currentSlug: string;
  titleImage: string;
};

type Blog = {
  title: string;
  description: string;
  currentSlug: string;
  titleImage: string;
  content;
};
